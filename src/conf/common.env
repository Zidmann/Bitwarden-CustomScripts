# Definition of the date and time when the script is launched
TODAYDATE="$(date +%Y%m%d)"
TODAYTIME="$(date +%H%M%S)"

# Information about the script directories used
BAT_DIR="$DIRNAME/bat"
CONF_DIR="$DIRNAME/conf"
DATA_DIR="$DIRNAME/data"
LOG_DIR="$DIRNAME/log"
SENT_DATA_DIR="$DATA_DIR/sent"
TMP_DIR="$DIRNAME/tmp"
UTIL_DIR="$DIRNAME/util"

# Bitwarden directories
BW_DIR="/home/bitwarden"
BW_DATA="$BW_DIR/bwdata"

# The working files 
SECURE_KEY_FILENAME="bitwarden.pub"
ARCHIVE_FILENAME="bitwarden.$TODAYDATE.$TODAYTIME.tar.gz"
ENCRYPTED_ARCHIVE_FILENAME="$ARCHIVE_FILENAME.enc"
AES_KEY_FILENAME="pass.$TODAYDATE.$TODAYTIME.bin"
ENCRYPTED_AES_KEY_FILENAME="$AES_KEY_FILENAME.enc"

# The Google Cloud Platform configuration
GCP_CONF_FILE="$CONF_DIR/gcp.conf"
ENCRYPTED_ARCHIVE_FILEPATTERN="bitwarden.????????.??????.tar.gz.enc"
ENCRYPTED_AES_KEY_FILEPATTERN="pass.????????.??????.bin.enc"

# The retention period
RETENTION_PERIOD=365

